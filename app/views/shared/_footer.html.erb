<div id="footer">
  <div class="container">
    <div class="row">
      <div class="footer-infos col-xs-12 col-md-4">
        <h2><%=t(:infos)%></h2>
<!--         <% if user_signed_in? %>
          <%= link_to destroy_user_session_path, class: "top-nav-link", method: :delete do %>
            <span class="info-text info-text-3"><%=t(:unconnect)%></span>
          <% end %>
        <% else %>
          <%= link_to new_user_session_path, title: "Accès à votre compte", class: "navbar-item navbar-link top-nav-link" do %>
            <span class="info-text info-text-3"><%=t(:connect)%></span>
          <% end %>
        <% end %> -->
        <% if user_signed_in? %>
          <% if current_user.admin %>
            <%= link_to admin_root_path do %>
              <span>Administration</span>
            <% end %>
          <% end %>
        <% end %>
        <%= link_to t(:product_return), info_path %>
        <%= link_to t(:deliveries), info_path %>
        <%= link_to t(:cgv), cgv_path %>
        <%= link_to t(:legal), legal_path %>
        <%= link_to t(:contact), contact_path %>
      </div>
      <div class="newsletter-subscribe col-xs-12 col-md-8">
        <h2><%= t(:new_and_updates).upcase%></h2>
        <p><%=t(:yes_i_want)%></p>
        <%= render 'shared/subscribe' %>
        <%= render 'shared/social_nav_footer' %>
      </div>
      <div class="mylink">
        <%= link_to @dev_redirection || session[:dev_redirection_default], title: "Créez votre boutique en ligne avec l'agence de Guillaume Crouillere" do %>
          <%= image_tag  'lego.svg', alt: "Guillaume Crouillere - Developpeur WEB indépendant"%>
      <% end %>
      </div>
    </div>
  </div>
</div>
